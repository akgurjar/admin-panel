

// declare
namespace Listing {
    export interface Options {
        search?: boolean;
        filter?: any;
    }
    export interface Config {
        label: string;
        options?: Options;
    }
}
